/**
 * Core security classes. Spring security is implemented as a filter chain that works independently from the
 * Dispatcher Servlet. Subsequently all security related classes must be in the root application context.
 *
 * To simplify component scanning, this package contains both the components needed for web security and api security.
 */
package io.yodo.pragphil.core.security;

